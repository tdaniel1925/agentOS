/**
 * Calendar Setup Page
 * Wrapper that loads the client component
 */

import CalendarSetupClientPage from './client-page'

export default function CalendarSetupPage() {
  return <CalendarSetupClientPage />
}
